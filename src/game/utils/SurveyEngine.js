/**
 * 게임의 모든 치수(크기, 위치, 비율 등)를 정의하고 관리하는 엔진.
 * 이 파일의 값만 수정하면 게임의 전체 레이아웃이 조절됩니다.
 */
export const surveyEngine = {
    // --- 캔버스 기본 치수 ---
    canvas: {
        width: 1024,
        height: 768
    },

    // --- 영지 그리드 관련 치수 ---
    territoryGrid: {
        cols: 3, // 3열
        rows: 3, // 3행
        x: 180,  // 그리드 전체가 시작되는 x좌표
        y: 250,  // 그리드 전체가 시작되는 y좌표
        cellWidth: 220,  // 각 칸의 너비
        cellHeight: 150, // 각 칸의 높이
    },

    // --- 건물 아이콘 관련 치수 ---
    buildingIcon: {
        // 아이콘이 그리드 셀 안에서 차지할 비율 (0.8 = 80%)
        // 이 값을 조절하여 아이콘 크기를 변경할 수 있습니다.
        scaleToCell: 0.8, 
    }
};
